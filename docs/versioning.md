# 版本管理与变更记录

> 状态：生效中（V2.5 起）
> 配套文件：[Q-Nexus项目变更说明.xlsx](../Q-Nexus项目变更说明.xlsx)（项目目录下的变更说明表）、仓库根目录 `VERSION`

## 规则

1. **Superpower 需求升主版本**：所有 @Superpower 需求（由技能驱动，如 `/improve-codebase-architecture`、`/implement`、`/code-review`）完成后必须升主版本号，例如 V1.0 → V2.0，并写明该版本的变更摘要与验收证据。
2. **小型修改升小版本**：用户直接要求的小型修改（缺陷修复、文案或布局调整等）按 V1.1 → V1.2 递增后缀，同样写明变更摘要。
3. **每次变更附变更说明表**：项目目录下的 `Q-Nexus项目变更说明.xlsx` 必须为每次变更追加一行，字段：版本号、日期、变更类型（Superpower 需求／小型修改／数据变更／环境变更／交付里程碑）、变更摘要、关联提交、验收证据。
4. **代码同步 Git 并可回溯**：所有代码变更提交到当前分支并推送 GitHub；每个版本打对应标签（`v1.0`、`v1.1`、`v2.0`…），保证任意版本可完整回溯。
5. **版本号落文件**：仓库根目录 `VERSION` 保存当前版本号，与 Git 标签、变更说明表保持一致。

## 版本历史

| 版本 | 日期 | 类型 | 摘要 | 标签 |
| --- | --- | --- | --- | --- |
| V1.0 | 2026-08-20 | 交付里程碑 | Slice 1–6 全部交付 + Mac Studio 本地部署 | `v1.0` |
| V1.1 | 2026-08-20 | 小型修改 | 后台「管理」入口；主题改为单按钮浅/深色切换 | `v1.1` |
| V2.0 | 2026-08-21 | Superpower 需求 | 架构深化：archival 归档生命周期深模块（C1）、access 权限模块合并（C2） | `v2.0` |
| V2.1 | 2026-08-21 | 小型修改 | 首页 Editorial Space 主题适配图形与布局（PR #58/#59） | `v2.1` |
| V2.2 | 2026-08-22 | 小型修改 | 修复数据卷属主导致的 EACCES；补齐 lucide-react 依赖 | `v2.2` |
| V2.3 | 2026-08-22 | 小型修改 | 放开代理请求体上限，支持 500MB 以内模板上传（TPL-06） | `v2.3` |
| V2.4 | 2026-08-22 | 小型修改 | 修复超长文件名导致模板扫描/发布按钮被遮挡 | `v2.4` |
| V2.4 | 2026-08-22/24 | 数据与环境变更 | 品质知识演示内容与模板发布；有线网卡 IP 变更排查 | — |
| V2.5 | 2026-09-17 | 小型修改 | 建立版本号、变更说明表与 Git 标签规则 | `v2.5` |
| V2.6 | 2026-09-17 | 小型修改 | 修复代理缓存旧 IP 导致界面 502：nginx 改用 Docker 内嵌 DNS 重新解析后端 | `v2.6` |
| V2.6 | 2026-09-17 | 数据变更 | 部署库新增三种权限的测试账号：编辑者 `editor`、阅读者 `reader`（首登强制改密） | — |
| V2.6 | 2026-09-17 | 环境变更 | 新增 `scripts/configure-wired-lan-ip.sh` 向导：把门户局域网地址固定在有线以太网（en0 = 192.168.60.152），避免 DHCP 换 IP 导致访问失效 | — |
| V2.7 | 2026-09-17 | 小型修改 | 修复权限缺陷：新人路线管理（新人专区）原先仅管理员可用，现按需求开放给编辑者（阅读者仍拒绝） | `v2.7` |
| V2.8 | 2026-09-17 | 小型修改 | 编辑者管理落地页补齐「新人路线管理」「栏目与主题管理」入口；栏目与主题管理按用户要求开放给编辑者，并同步更新需求文档角色表与 CONTEXT 职责描述 | `v2.8` |
| V2.9 | 2026-09-17 | 小型修改 | 补齐文章维护闭环：新增「文章管理」列表页与入口；「新建文章」页接上创建草稿动作（此前保存无反应）；编辑器新增「知识别名」字段；内容负责人改为账号下拉；保存后显示提示条 | `v2.9` |
| V3.0 | 2026-09-18 | Superpower 需求 | 新人路线改为一篇文章对应一步的可编辑列表，支持搜索添加、新建、编辑、排序和移出；原阶段内容与链接无损迁移，数量不再固定为六步；文章创建与路线加入同一事务，草稿与发布保留上一版 | `v3.0` |
| V3.1 | 2026-09-18 | 小型修改 | 草稿箱补齐发布入口：文章管理列表的草稿行与新人路线列表的草稿行新增「发布」按钮，不必先进编辑器即可发布；发布缺必填项时按缺项提示且状态不变，案例文章提示先到编辑器确认脱敏，发布成功后刷新阅读侧 | `v3.1` |
| V3.2 | 2026-09-18 | 小型修改 | 主题页改为整篇可读：品质知识/散热知识的主题页内联渲染每篇已发布文章的完整正文（此前只显示标题与摘要，需再点一次才能阅读），标题改为可见链接、并给出「打开阅读页（目录 / 收藏 / 反馈）」入口；正文渲染抽成阅读页与主题页共用的 ArticleBody 组件；同时修复正文内图片无宽度约束导致整页横向滚动的问题（阅读页与主题页一并生效）；草稿与已归档文章仍不进入阅读侧 | `v3.2` |
| V3.3 | 2026-09-18 | 小型修改 | 主题页信息层级精简：去掉正文区重复的栏目名（品质知识）、主题名、文章标题与摘要，只保留「负责人 · 更新时间 · 打开阅读页（目录 / 收藏 / 反馈）」一行加整篇正文（栏目与主题已由顶部导航和左侧分类树表达）；栏目名/主题名/文章标题仍以屏幕阅读器可见的方式保留文档结构 | `v3.3` |
| V3.4 | 2026-09-20 | 小型修改 | 文章管理列表补齐归档入口：每行可直接填写归档原因并归档（此前归档藏在版本历史页，列表里找不到），归档后立即从阅读侧与列表中撤下并进入回收站（DEL-01/DEL-02）；管理员在文章管理页可直接进入回收站；归档后行内不再显示归档表单，已归档项在「已归档」筛选下可查 | `v3.4` |

## 每次变更的操作清单

本次未在开发电脑执行正式部署。目标机器升级前请备份数据库与受控文件，并按 [新人路线升级说明](deployment.md#v30-新人路线升级) 在备份副本验证迁移。

1. 判定类型：Superpower 需求 → 主版本 +1；直接小型修改 → 小版本 +1；纯数据/环境变更沿用当前版本号并在类型列注明。
2. 更新 `VERSION`。
3. 在 `Q-Nexus项目变更说明.xlsx`「版本总览」追加一行，必要时在「提交对照」补充提交记录。
4. 提交并推送代码。
5. 为该版本打标签并推送：`git tag v<版本> && git push origin v<版本>`（网络受限时可用 GitHub API 创建同名标签）。
