# Markdown 正文存入 PostgreSQL，文件资源独立保存

知识文章以 Markdown 表达，但其正文与版本和业务元数据一起存入 PostgreSQL，数据库是网页编辑场景下的唯一真实来源；图片和任意格式的模板文件保存到受控数据目录，数据库只记录元数据与校验值。相比直接把 `.md` 文件或二进制文件作为唯一来源，这一选择更容易保证权限、版本历史、并发编辑、搜索和审计，同时通过 Markdown 导入、导出和完整备份保持可迁移性。
